procs:
  Host:
    shell: pnpm --filter host start --platform ios
    stop: SIGKILL
  App1:
    shell: pnpm --filter app1 start --platform ios
    stop: SIGKILL
  App2:
    shell: pnpm --filter app2 start --platform ios
    stop: SIGKILL